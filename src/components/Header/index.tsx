// == Imports

import React, { FC } from 'react';

import './header.scss';

// == Component

const Header: FC = () => {
  return (
      <header className="header">
        <h1>To-Do List</h1>
      </header>
  );
}

// == Export

export default Header;
